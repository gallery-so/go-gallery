# GCP app.yaml reference: https://cloud.google.com/appengine/docs/standard/python/config/appref

runtime: go115 # gcp doesn't support go 1.16 yet

main: ./cmd/server

env_variables:
  GLRY_ENV: development
  GLRY_PORT: 8080

  # TODO: use secrets manager in prod
  GLRY_MONGO_URL: mongodb+srv://glry-dev:OlLAbxBTyVicDND1@gallery-cluster.2anpm.mongodb.net/myFirstDatabase?retryWrites=true&w=majority
  GLRY_MONGO_DB_NAME: gallery
  # TODO: use SSL in prod
  GLRY_MONGO_SSL_CA_FILE_PATH: ""

  GLRY_SENTRY_ENDPOINT: ""
  GLRY_JWT_TOKEN_TTL_SECS: 259200
