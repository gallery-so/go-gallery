version: "3.9"
services:
  redis:
    image: "redis:6"
    ports:
      - "6379:6379"
  postgres:
    build:
      context: "."
      dockerfile: "docker/postgres/DOCKERFILE"
      args:
        - PGHOST=${PGHOST}
        - PGPORT=${PGPORT}
        - PGUSER=${PGUSER}
        - PGPASSWORD=${PGPASSWORD}
        - PGDATABASE=${PGDATABASE}
        - PGTESTUSER=${PGTESTUSER}
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_HOST_AUTH_METHOD=trust
      - POSTGRES_USER=postgres
      - POSTGRES_DB=postgres
  postgres_indexer:
    build:
      context: "."
      dockerfile: "docker/postgres_indexer/DOCKERFILE"
      args:
        - PGHOST=${PGHOST}
        - PGPORT=${PGPORT}
        - PGUSER=${PGUSER}
        - PGPASSWORD=${PGPASSWORD}
        - PGDATABASE=${PGDATABASE}
        - PGTESTUSER=${PGTESTUSER}
    ports:
      - "5433:5432"
    environment:
      - POSTGRES_HOST_AUTH_METHOD=trust
      - POSTGRES_USER=postgres
      - POSTGRES_DB=postgres
  # Used to proxy cloud tasks requests
  nginx:
    image: nginx
    ports:
      - "8080:80"
    volumes:
      - ${PWD}/docker/nginx/nginx.local.conf:/etc/nginx/nginx.conf
  # If you're making changes to the mediaprocessing image, you'll want
  # to uncomment the lines below so that you can test your changes made
  # to the container.
  #
  # Most of the time, you'll probably just want to run mediaprocessing
  # on your host i.e. `go run cmd/mediaprocessing/main.go`
  # mediaprocessing:
  #   build:
  #     context: "."
  #     dockerfile: "docker/mediaprocessing/Dockerfile"
  #   ports:
  #     - "6500:6500"
  #   env_file:
  #     - _local/app-docker-mediaprocessing.env
  #   volumes:
  #     - ${PWD}/_local:/app/_local
  #     - ${PWD}/_deploy:/app/_deploy
  #   depends_on:
  #     - postgres
  #     - redis
