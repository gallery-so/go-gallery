// Code generated by github.com/gallery-so/dataloaden, DO NOT EDIT.

package dataloader

import (
	"context"
	"sync"
	"time"

	"github.com/mikeydub/go-gallery/db/gen/coredb"
	"github.com/mikeydub/go-gallery/service/persist"
)

type CommentLoaderByIDSettings interface {
	getContext() context.Context
	getWait() time.Duration
	getMaxBatchOne() int
	getMaxBatchMany() int
	getDisableCaching() bool
	getPublishResults() bool
	getSubscriptionRegistry() *[]interface{}
	getMutexRegistry() *[]*sync.Mutex
}

// CommentLoaderByIDCacheSubscriptions
type CommentLoaderByIDCacheSubscriptions struct {
	// AutoCacheWithKey is a function that returns the persist.DBID cache key for a coredb.Comment.
	// If AutoCacheWithKey is not nil, this loader will automatically cache published results from other loaders
	// that return a coredb.Comment. Loaders that return pointers or slices of coredb.Comment
	// will be dereferenced/iterated automatically, invoking this function with the base coredb.Comment type.
	AutoCacheWithKey func(coredb.Comment) persist.DBID

	// AutoCacheWithKeys is a function that returns the []persist.DBID cache keys for a coredb.Comment.
	// Similar to AutoCacheWithKey, but for cases where a single value gets cached by many keys.
	// If AutoCacheWithKeys is not nil, this loader will automatically cache published results from other loaders
	// that return a coredb.Comment. Loaders that return pointers or slices of coredb.Comment
	// will be dereferenced/iterated automatically, invoking this function with the base coredb.Comment type.
	AutoCacheWithKeys func(coredb.Comment) []persist.DBID

	// TODO: Allow custom cache functions once we're able to use generics. It could be done without generics, but
	// would be messy and error-prone. A non-generic implementation might look something like:
	//
	//   CustomCacheFuncs []func(primeFunc func(key, value)) func(typeToRegisterFor interface{})
	//
	// where each CustomCacheFunc is a closure that receives this loader's unsafePrime method and returns a
	// function that accepts the type it's registering for and uses that type and the unsafePrime method
	// to prime the cache.
}

func (l *CommentLoaderByID) setContext(ctx context.Context) {
	l.ctx = ctx
}

func (l *CommentLoaderByID) setWait(wait time.Duration) {
	l.wait = wait
}

func (l *CommentLoaderByID) setMaxBatch(maxBatch int) {
	l.maxBatch = maxBatch
}

func (l *CommentLoaderByID) setDisableCaching(disableCaching bool) {
	l.disableCaching = disableCaching
}

func (l *CommentLoaderByID) setPublishResults(publishResults bool) {
	l.publishResults = publishResults
}

// NewCommentLoaderByID creates a new CommentLoaderByID with the given settings, functions, and options
func NewCommentLoaderByID(
	settings CommentLoaderByIDSettings, fetch func(ctx context.Context, keys []persist.DBID) ([]coredb.Comment, []error),
	funcs CommentLoaderByIDCacheSubscriptions,
	opts ...func(interface {
		setContext(context.Context)
		setWait(time.Duration)
		setMaxBatch(int)
		setDisableCaching(bool)
		setPublishResults(bool)
	}),
) *CommentLoaderByID {
	loader := &CommentLoaderByID{
		ctx:                  settings.getContext(),
		wait:                 settings.getWait(),
		disableCaching:       settings.getDisableCaching(),
		publishResults:       settings.getPublishResults(),
		subscriptionRegistry: settings.getSubscriptionRegistry(),
		mutexRegistry:        settings.getMutexRegistry(),
		maxBatch:             settings.getMaxBatchOne(),
	}

	for _, opt := range opts {
		opt(loader)
	}

	// Set this after applying options, in case a different context was set via options
	loader.fetch = func(keys []persist.DBID) ([]coredb.Comment, []error) { return fetch(loader.ctx, keys) }

	if loader.subscriptionRegistry == nil {
		panic("subscriptionRegistry may not be nil")
	}

	if loader.mutexRegistry == nil {
		panic("mutexRegistry may not be nil")
	}

	if !loader.disableCaching {
		// One-to-one mappings: cache one value with one key
		if funcs.AutoCacheWithKey != nil {
			cacheFunc := func(t coredb.Comment) {
				loader.unsafePrime(funcs.AutoCacheWithKey(t), t)
			}
			loader.registerCacheFunc(&cacheFunc, &loader.mu)
		}

		// One-to-many mappings: cache one value with many keys
		if funcs.AutoCacheWithKeys != nil {
			cacheFunc := func(t coredb.Comment) {
				keys := funcs.AutoCacheWithKeys(t)
				for _, key := range keys {
					loader.unsafePrime(key, t)
				}
			}
			loader.registerCacheFunc(&cacheFunc, &loader.mu)
		}
	}

	return loader
}

// CommentLoaderByID batches and caches requests
type CommentLoaderByID struct {
	// context passed to fetch functions
	ctx context.Context

	// this method provides the data for the loader
	fetch func(keys []persist.DBID) ([]coredb.Comment, []error)

	// how long to wait before sending a batch
	wait time.Duration

	// this will limit the maximum number of keys to send in one batch, 0 = no limit
	maxBatch int

	// whether this dataloader will cache results
	disableCaching bool

	// whether this dataloader will publish its results for others to cache
	publishResults bool

	// a shared slice where dataloaders will register and invoke caching functions.
	// the same slice should be passed to every dataloader.
	subscriptionRegistry *[]interface{}

	// a shared slice, parallel to the subscription registry, that holds a reference to the
	// cache mutex for the subscription's dataloader
	mutexRegistry *[]*sync.Mutex

	// INTERNAL

	// lazily created cache
	cache map[persist.DBID]coredb.Comment

	// typed cache functions
	//subscribers []func(coredb.Comment)
	subscribers []commentLoaderByIDSubscriber

	// functions used to cache published results from other dataloaders
	cacheFuncs []interface{}

	// the current batch. keys will continue to be collected until timeout is hit,
	// then everything will be sent to the fetch method and out to the listeners
	batch *commentLoaderByIDBatch

	// mutex to prevent races
	mu sync.Mutex

	// only initialize our typed subscription cache once
	once sync.Once
}

type commentLoaderByIDBatch struct {
	keys    []persist.DBID
	data    []coredb.Comment
	error   []error
	closing bool
	done    chan struct{}
}

// Load a Comment by key, batching and caching will be applied automatically
func (l *CommentLoaderByID) Load(key persist.DBID) (coredb.Comment, error) {
	return l.LoadThunk(key)()
}

// LoadThunk returns a function that when called will block waiting for a Comment.
// This method should be used if you want one goroutine to make requests to many
// different data loaders without blocking until the thunk is called.
func (l *CommentLoaderByID) LoadThunk(key persist.DBID) func() (coredb.Comment, error) {
	l.mu.Lock()
	if !l.disableCaching {
		if it, ok := l.cache[key]; ok {
			l.mu.Unlock()
			return func() (coredb.Comment, error) {
				return it, nil
			}
		}
	}
	if l.batch == nil {
		l.batch = &commentLoaderByIDBatch{done: make(chan struct{})}
	}
	batch := l.batch
	pos := batch.keyIndex(l, key)
	l.mu.Unlock()

	return func() (coredb.Comment, error) {
		<-batch.done

		var data coredb.Comment
		if pos < len(batch.data) {
			data = batch.data[pos]
		}

		var err error
		// its convenient to be able to return a single error for everything
		if len(batch.error) == 1 {
			err = batch.error[0]
		} else if batch.error != nil {
			err = batch.error[pos]
		}

		if err == nil {
			if !l.disableCaching {
				l.mu.Lock()
				l.unsafeSet(key, data)
				l.mu.Unlock()
			}

			if l.publishResults {
				l.publishToSubscribers(data)
			}
		}

		return data, err
	}
}

// LoadAll fetches many keys at once. It will be broken into appropriate sized
// sub batches depending on how the loader is configured
func (l *CommentLoaderByID) LoadAll(keys []persist.DBID) ([]coredb.Comment, []error) {
	results := make([]func() (coredb.Comment, error), len(keys))

	for i, key := range keys {
		results[i] = l.LoadThunk(key)
	}

	comments := make([]coredb.Comment, len(keys))
	errors := make([]error, len(keys))
	for i, thunk := range results {
		comments[i], errors[i] = thunk()
	}
	return comments, errors
}

// LoadAllThunk returns a function that when called will block waiting for a Comments.
// This method should be used if you want one goroutine to make requests to many
// different data loaders without blocking until the thunk is called.
func (l *CommentLoaderByID) LoadAllThunk(keys []persist.DBID) func() ([]coredb.Comment, []error) {
	results := make([]func() (coredb.Comment, error), len(keys))
	for i, key := range keys {
		results[i] = l.LoadThunk(key)
	}
	return func() ([]coredb.Comment, []error) {
		comments := make([]coredb.Comment, len(keys))
		errors := make([]error, len(keys))
		for i, thunk := range results {
			comments[i], errors[i] = thunk()
		}
		return comments, errors
	}
}

// Prime the cache with the provided key and value. If the key already exists, no change is made
// and false is returned.
// (To forcefully prime the cache, clear the key first with loader.clear(key).prime(key, value).)
func (l *CommentLoaderByID) Prime(key persist.DBID, value coredb.Comment) bool {
	if l.disableCaching {
		return false
	}
	l.mu.Lock()
	var found bool
	if _, found = l.cache[key]; !found {
		l.unsafeSet(key, value)
	}
	l.mu.Unlock()
	return !found
}

// Prime the cache without acquiring locks. Should only be used when the lock is already held.
func (l *CommentLoaderByID) unsafePrime(key persist.DBID, value coredb.Comment) bool {
	if l.disableCaching {
		return false
	}
	var found bool
	if _, found = l.cache[key]; !found {
		l.unsafeSet(key, value)
	}
	return !found
}

// Clear the value at key from the cache, if it exists
func (l *CommentLoaderByID) Clear(key persist.DBID) {
	if l.disableCaching {
		return
	}
	l.mu.Lock()
	delete(l.cache, key)
	l.mu.Unlock()
}

func (l *CommentLoaderByID) unsafeSet(key persist.DBID, value coredb.Comment) {
	if l.cache == nil {
		l.cache = map[persist.DBID]coredb.Comment{}
	}
	l.cache[key] = value
}

// keyIndex will return the location of the key in the batch, if its not found
// it will add the key to the batch
func (b *commentLoaderByIDBatch) keyIndex(l *CommentLoaderByID, key persist.DBID) int {
	for i, existingKey := range b.keys {
		if key == existingKey {
			return i
		}
	}

	pos := len(b.keys)
	b.keys = append(b.keys, key)
	if pos == 0 {
		go b.startTimer(l)
	}

	if l.maxBatch != 0 && pos >= l.maxBatch-1 {
		if !b.closing {
			b.closing = true
			l.batch = nil
			go b.end(l)
		}
	}

	return pos
}

func (b *commentLoaderByIDBatch) startTimer(l *CommentLoaderByID) {
	time.Sleep(l.wait)
	l.mu.Lock()

	// we must have hit a batch limit and are already finalizing this batch
	if b.closing {
		l.mu.Unlock()
		return
	}

	l.batch = nil
	l.mu.Unlock()

	b.end(l)
}

func (b *commentLoaderByIDBatch) end(l *CommentLoaderByID) {
	b.data, b.error = l.fetch(b.keys)
	close(b.done)
}

type commentLoaderByIDSubscriber struct {
	cacheFunc func(coredb.Comment)
	mutex     *sync.Mutex
}

func (l *CommentLoaderByID) publishToSubscribers(value coredb.Comment) {
	// Lazy build our list of typed cache functions once
	l.once.Do(func() {
		for i, subscription := range *l.subscriptionRegistry {
			if typedFunc, ok := subscription.(*func(coredb.Comment)); ok {
				// Don't invoke our own cache function
				if !l.ownsCacheFunc(typedFunc) {
					l.subscribers = append(l.subscribers, commentLoaderByIDSubscriber{cacheFunc: *typedFunc, mutex: (*l.mutexRegistry)[i]})
				}
			}
		}
	})

	// Handling locking here (instead of in the subscribed functions themselves) isn't the
	// ideal pattern, but it's an optimization that allows the publisher to iterate over slices
	// without having to acquire the lock many times.
	for _, s := range l.subscribers {
		s.mutex.Lock()
		s.cacheFunc(value)
		s.mutex.Unlock()
	}
}

func (l *CommentLoaderByID) registerCacheFunc(cacheFunc interface{}, mutex *sync.Mutex) {
	l.cacheFuncs = append(l.cacheFuncs, cacheFunc)
	*l.subscriptionRegistry = append(*l.subscriptionRegistry, cacheFunc)
	*l.mutexRegistry = append(*l.mutexRegistry, mutex)
}

func (l *CommentLoaderByID) ownsCacheFunc(f *func(coredb.Comment)) bool {
	for _, cacheFunc := range l.cacheFuncs {
		if cacheFunc == f {
			return true
		}
	}

	return false
}
